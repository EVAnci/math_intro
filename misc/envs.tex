% --- Custom envs ---
% Usage: \CreateBlock{env_name}{Title}{Color}{Counter style}
% Counter style: can be 'arabic', 'alph', 'Roman', etc.

\newcommand{\CreateBlock}[4]{
  % 1. Create the counter
  \newcounter{#1}[section]
  
  % 2. The counter format
  % \expandafter is used to tell \csname to execute before \renewcommand
  \expandafter\renewcommand\csname the#1\endcsname{%
    \thesection.\csname #4\endcsname{#1}%
  }
  
  % 3. tcolorbox definition
  \newtcolorbox{#1}[1][]{
    enhanced, before skip=2mm, after skip=2mm,
    colback=black!5, colframe=black!50, boxrule=0.2mm,
    attach boxed title to top left={xshift=1cm,yshift*=1mm-\tcboxedtitleheight},
    varwidth boxed title*=-3cm,
    boxed title style={frame code={
      \path[fill=tcbcolback!30!black]
        ([yshift=-1mm,xshift=-1mm]frame.north west)
          arc[start angle=0, end angle=180, radius=1mm]
        ([yshift=-1mm,xshift=1mm]frame.north east)
          arc[start angle=180, end angle=0, radius=1mm];
      \path[left color=tcbcolback!80!black,right color=tcbcolback!60!black,
        middle color=tcbcolback!80!black]
        ([xshift=-2mm]frame.north west) -- ([xshift=2mm]frame.north east)
        [rounded corners=1mm]-- ([xshift=1mm,yshift=-1mm]frame.north east) 
        -- (frame.south east) -- (frame.south west)
        -- ([xshift=-1mm,yshift=-1mm]frame.north west)
        [sharp corners]--cycle;
      },interior engine=empty,
    },
    fonttitle=\scriptsize,
    title={#2 \csname the#1\endcsname\ifstrempty{##1}{}{: ##1}},
    colbacktitle=#3,
    separator sign=none,
    code={\refstepcounter{#1}},
    breakable,
    parbox=false
  }
}

\CreateBlock{example}{Ejemplo}{teal!70}{Alph}
\CreateBlock{definitionbox}{Definición}{orange}{arabic}
\CreateBlock{corollary}{Corolario}{blue!30!gray}{arabic}
\CreateBlock{proposition}{Proposición}{red!30!gray}{arabic}
\CreateBlock{remark}{Aclaración}{green!30!gray}{arabic}
\CreateBlock{property}{Propiedad}{yellow!50!gray}{arabic}

% Apart from the avobe tcolorbox envs, the following are more simple envs

% This avoids the italic font in the environments
\theoremstyle{definition}
\newtheorem{definition}{Definición}[section]
\addto\captionsspanish{\renewcommand{\proofname}{Demostración}}
  
\newtheorem{theorem}{Teorema}[section]

% ------------------------------------------------------------------------------
% tcolorbox envs to avoid repeating code

\newtcolorbox{note}[1][]{
    enhanced,
    breakable,
    frame hidden,
    sharp corners,
    colback=black!5,
    coltext=black!60,
    borderline west={3pt}{0pt}{teal}, 
    boxsep=5pt,
    left=10pt,
    before skip=10pt,
    after skip=10pt,
    #1 % --- extra args ---
}
